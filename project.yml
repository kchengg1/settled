# XcodeGen spec — run `xcodegen` in the repo root to produce SplitChecks.xcodeproj.
# Install XcodeGen with `brew install xcodegen`.
name: SplitChecks
options:
  bundleIdPrefix: com.kchengg1
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "15.0"

packages:
  SplitChecksCore:
    path: SplitChecksCore

targets:
  SplitChecks:
    type: application
    platform: iOS
    sources:
      - App
    dependencies:
      - package: SplitChecksCore
    # An explicit Info.plist (rather than the generated one) because the
    # shared-group file type needs nested declarations that the
    # INFOPLIST_KEY_* build settings can't express.
    info:
      path: SplitChecks-Info.plist
      properties:
        # Standard Apple frameworks only (HTTPS-level crypto) — exempt, so
        # TestFlight skips the export-compliance question on every build.
        ITSAppUsesNonExemptEncryption: false
        NSCameraUsageDescription: "Split Checks uses the camera to scan receipts. Photos are processed on this device and never leave it."
        UILaunchScreen: {}
        UISupportedInterfaceOrientations: [UIInterfaceOrientationPortrait]
        # Groups shared as files: we own the type, so we export it and
        # register as its editor.
        UTExportedTypeDeclarations:
          - UTTypeIdentifier: com.kchengg1.splitchecks.group
            UTTypeDescription: Split Checks Group
            UTTypeConformsTo: [public.json]
            UTTypeTagSpecification:
              public.filename-extension: [splitchecks]
        CFBundleDocumentTypes:
          - CFBundleTypeName: Split Checks Group
            CFBundleTypeRole: Editor
            LSHandlerRank: Owner
            LSItemContentTypes: [com.kchengg1.splitchecks.group]
        # Required to accept an invitation to a group shared through iCloud.
        CKSharingSupported: true
    # Live group sharing runs on the user's own iCloud. The container must
    # exist in the Apple developer account before a signed build (see README).
    entitlements:
      path: SplitChecks.entitlements
      properties:
        com.apple.developer.icloud-container-identifiers: [iCloud.com.kchengg1.splitchecks]
        com.apple.developer.icloud-services: [CloudKit]
    settings:
      base:
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        GENERATE_INFOPLIST_FILE: NO
        PRODUCT_BUNDLE_IDENTIFIER: com.kchengg1.splitchecks
        SWIFT_VERSION: "5.9"
        TARGETED_DEVICE_FAMILY: "1"
        CURRENT_PROJECT_VERSION: 1
        MARKETING_VERSION: 0.1.0

  SplitChecksUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - SplitChecksUITests
    dependencies:
      - target: SplitChecks
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: com.kchengg1.splitchecks.uitests
        TEST_TARGET_NAME: SplitChecks
        SWIFT_VERSION: "5.9"

# Explicit scheme so the UI-test target is available to `xcodebuild test`
# (screenshots) while the plain app build/archive is unchanged — the test
# target only participates in the test action.
schemes:
  SplitChecks:
    build:
      targets:
        SplitChecks: all
        SplitChecksUITests: [test]
    test:
      targets:
        - SplitChecksUITests
    run:
      config: Debug
    archive:
      config: Release

